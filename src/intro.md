こんにちは！これは `src` フォルダから直接読み込まれたMarkdownファイルです。

## コードの色分け（Prism.js）
JavaScriptの構文が自動認識され、綺麗にハイライトされます。

```js
const a = 1;
const b = 2;

function calc(x, y) {
    return x + y;
}

console.log(calc(a, b));
```
